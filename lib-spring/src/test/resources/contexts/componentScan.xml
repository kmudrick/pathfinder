<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:context="http://www.springframework.org/schema/context"
    xmlns:mvc="http://www.springframework.org/schema/mvc"
    xsi:schemaLocation="http://www.springframework.org/schema/beans   http://www.springframework.org/schema/beans/spring-beans.xsd
                        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
                        http://www.springframework.org/schema/mvc     http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- this context file just contains a bunch of component scans, exercising different ways to specify -->

    <!-- the easiest version: a scan that only specifies one package -->
    <context:component-scan base-package="com.example.pkg1"/>

    <!-- harder: a comma-delimited list with whitespace -->
    <context:component-scan base-package="com.example.pkg2, com.example.pkg3,
                                          com.example.pkg4"/>

    <!-- and, just to make sure that we don't blow up with dupes -->
    <context:component-scan base-package="com.example.pkg3"/>

</beans>
